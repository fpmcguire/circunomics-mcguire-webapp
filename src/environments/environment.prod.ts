export const environment = {
  production: true,
  /**
   * Set GITHUB_TOKEN via your deployment platform's environment/secrets configuration.
   * Never hardcode a token here.
   */
  githubToken: '',
};
